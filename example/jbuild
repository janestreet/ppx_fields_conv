(executables
 ((names (test))
  (libraries (fieldslib))
  (preprocess (pps (ppx_type_conv ppx_fields_conv)))))

(alias ((name DEFAULT)
        (deps (test.ml.pp test.mli.pp))))

(jbuild_version jane_street)
